# Build output
build/

# Model weights fetched at configure time (see tools/fetch_models.cmake).
# Redundant with build/ above, but listed explicitly per the S0-01 spec.
build/_models/

# macOS
.DS_Store

# IDE / editor files
.vscode/
.idea/
*.swp
*.swo
*~

# Claude Code machine-local overrides. `.claude/commands/` and any future
# shared `.claude/settings.json` are tracked (CLAUDE.md §8 points at
# `.claude/commands/review.md`); per-developer permission allowlists and
# env overrides belong in `settings.local.json` and stay untracked.
.claude/settings.local.json
