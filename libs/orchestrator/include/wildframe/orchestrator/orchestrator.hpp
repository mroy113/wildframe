#pragma once

/// \file
/// Public umbrella header for `wildframe_orchestrator` (Module 6).
///
/// Scope per handoff doc §10: owns the job queue and pipeline order,
/// provides the stage-registration interface, writes the per-batch JSON
/// manifest, and isolates per-image errors. Concrete APIs land with tasks
/// M6-01..M6-08.

namespace wildframe::orchestrator {

}  // namespace wildframe::orchestrator
